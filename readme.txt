SLIC_vectorization.py vectorizes remote sensing images, Train.py trains the classifier, 
and predict uses the trained classifier to predict, writes the categories into vector layers, 
and then draws them in software.